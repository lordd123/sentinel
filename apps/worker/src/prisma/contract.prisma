model Event {
  id        String   @id @default(uuid())
  type      String
  source    String
  severity  String
  createdAt DateTime @default(now())
}

model Alert {
  id        String   @id @default(uuid())
  eventId   String
  rule      String
  severity  String
  reason    String
  createdAt DateTime @default(now())
}